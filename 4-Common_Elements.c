#include <stdio.h>
#include <string.h>

int main()
{
    int l1[] = {2,5,5,5};
    int l2[] = {2,2,3,5,5,7};

    int common_ele;

    int len;

    if (strlen(l1)>strlen(l2))
    {
        len = strlen(l1);
    }

    else
    {
        len = strlen(l2);
    }


    for (i = 0 ; i<len ; i++)
    {
        if (l1[i] == l2[i])
    }

    return 0;
}

